# =====================================================
# Data Science TCB — pinned dependencies
# Python 3.11 required
# =====================================================
# Pin philosophy: lower bound enforces required features,
# upper bound prevents breaking major upgrades during project lifetime.
# Final lock with exact versions at end of Phase 0 (Tuần 2).

# -----------------------------------------------------
# Core numerical / data
# -----------------------------------------------------
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0
pyarrow>=17.0,<19.0

# -----------------------------------------------------
# Machine learning
# -----------------------------------------------------
scikit-learn>=1.5,<2.0
# torch: managed by conda (CUDA build). Do NOT install torch via pip in this env.
# See README "Setup" section for conda install commands.
shap>=0.46,<1.0

# -----------------------------------------------------
# Statistics
# -----------------------------------------------------
statsmodels>=0.14,<1.0
arch>=7.0,<8.0       # Stationary bootstrap (Politis-Romano 1994)

# -----------------------------------------------------
# Data sources (Vietnamese stock + global FX)
# -----------------------------------------------------
vnstock>=3.2          # v3 API: Vnstock().stock(symbol=..., source='VCI').quote.history(...)
yfinance>=0.2,<1.0

# vnstock v4+ requires a charting backend to import (design quirk of upstream lib).
# Install separately from vnstock's private index (NOT on PyPI):
#   pip install --extra-index-url https://vnstocks.com/api/simple vnstock_chart
# Fallback option (PyPI but historically has packaging bugs):
#   pip install vnstock_ezchart squarify wordcloud mplfinance
# We don't pin either here because the official one is off-PyPI; user installs once.

# -----------------------------------------------------
# Production / web
# -----------------------------------------------------
sqlalchemy>=2.0,<3.0
streamlit>=1.35,<2.0

# -----------------------------------------------------
# Dev / EDA — kept in main file for simplicity (academic project)
# -----------------------------------------------------
pytest>=8.0
jupyter>=1.0
ipykernel>=6.0
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66

# -----------------------------------------------------
# Optional but recommended
# -----------------------------------------------------
python-dotenv>=1.0    # Read .env for any secrets (none expected, but useful)
pytest-cov>=4.0